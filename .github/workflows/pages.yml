# Test build on GitHub Pages.
#
# This is the HAND-TEST deployment, not the release. CLAUDE.md section 17 says
# players get `main`; this exists because the game is designed for a 390x844
# phone and until now had only ever been played on a desktop. A build that
# cannot be opened on a phone cannot be tested on the device it is for.
#
# `dev` is deployed on purpose. `main` is six milestones behind, and the whole
# point is to test what was just written.
#
# The build runs the suite and the linter first. A red build must not become a
# URL somebody is testing against - a bug report from a broken deploy costs
# more than the deploy saves.

name: pages

on:
  push:
    branches: [dev, main]
  workflow_dispatch:

# One deploy at a time, and never cancel one half-finished: a cancelled Pages
# deployment can leave the site serving a partial build.
concurrency:
  group: pages
  cancel-in-progress: false

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      - run: npm run lint
      - run: npm test

      # `base: './'` in vite.config.js is what makes this work from
      # /<repo>/ rather than only from a domain root. It is relative on
      # purpose - see the comment there, it is a deploy-day bug otherwise.
      - run: npm run build

      # `enablement` is not optional here, and the failure it prevents is a
      # nasty one to read: with Pages not yet turned on for the repo, THIS
      # step still reports success, the build goes green, and the deploy job
      # then fails with **zero steps** - not even "Set up job". Nothing in the
      # log says why, because the job died resolving `environment:
      # github-pages`, which does not exist until a Pages site does.
      #
      # Turning it on from here also means a fresh clone or a fork deploys
      # without anybody visiting a settings page, which is the same argument
      # the rest of this file makes: a rule that lives only in a UI somebody
      # has to remember is not a rule.
      - uses: actions/configure-pages@v5
        with:
          enablement: true

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
